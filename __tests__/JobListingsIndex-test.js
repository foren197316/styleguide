'use strict';

jest.dontMock('../src/components/JobListingsIndex');

describe('JobListingsIndex', function () {
  it('shows a list of Job Listings', function () {
    // require('../src/js/main');

    // var React = require('../node_modules/react/addons');
    // var TestUtils = React.addons.TestUtils;

    // var JobListingsIndex = require('../src/components/JobListingsIndex');
    // var jobListingsIndex = TestUtils.renderIntoDocument(
      // JobListingsIndex({urls: {
        // jobListings: '/job_listings.json'
      // }})
    // );

    // var jobListings = TestUtils.scryRenderedDOMComponentsWithClass(jobListingsIndex, 'job-listing');
    // expect(jobListings.length).toBe(3);
  });
});
